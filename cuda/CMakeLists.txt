# ForgeRT CUDA Library

set(FORGERT_CUDA_SOURCES
    # CUDA kernel sources will be added here
)

# Create CUDA library (placeholder for now)
# add_library(forgert_cuda ${FORGERT_CUDA_SOURCES})
# target_include_directories(forgert_cuda PUBLIC ${FORGERT_INCLUDE_DIR})
# set_target_properties(forgert_cuda PROPERTIES CUDA_SEPARABLE_COMPILATION ON)

message(STATUS "ForgeRT CUDA library configured")
